CREATE TABLE enrollment_list (

    id int PRIMARY KEY AUTO_INCREMENT,
    school_year_start varchar(255) NOT NULL,
    school_year_end varchar(255) NOT NULL,
    start_date varchar(255) NOT NULL,
    end_date varchar(255) NOT NULL,
    
);